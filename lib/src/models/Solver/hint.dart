
import '../../components/components.dart';

class Hint {
  BlockType blockType;
  int place;
  int numOfRotations;

  Hint({
    required this.blockType,
    required this.place,
    required this.numOfRotations
  });
}